from setuptools import setup, find_packages

setup(
    name='benchmarkrewriter',
    version='0.1',
    description='A Python library for rewriting Job Shop Problem Benchmarks',
    author='Bianca Kasper',
    packages=find_packages(),
)